import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sowpay.mart',
  appName: 'Sowpay mart B2C Marketplace',
  webDir: 'public'
};

export default config;
